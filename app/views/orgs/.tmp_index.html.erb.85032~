<h1>Listing orgs</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Address</th>
    <th>Commendant</th>
  </tr>

<% @orgs.each do |org| %>
  <tr>
    <td><%=h org.name %></td>
    <td><%=h org.address %></td>
    <td><%=h org.commendant_id %></td>
	<td><%=h org.agents.size %></td>
    <td><%= link_to 'Show', org %></td>
    <td><%= link_to 'Edit', edit_org_path(org) %></td>
    <td><%= link_to 'Destroy', org, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New org', new_org_path %>